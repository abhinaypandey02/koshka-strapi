'use strict';

/**
 * claim-tag-page controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::claim-tag-page.claim-tag-page');
